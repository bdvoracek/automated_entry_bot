# Secrets — never commit
.env
.env.*
*.token
secrets/

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/

# OS / editor
.DS_Store

# Pipeline job state (runtime), but keep versioned unified-driver mappings
state/*
!state/unified_drivers_*.json
!state/causal_*.json
!state/gold_job.json
!state/b200_job.json

# Claude Code local settings (personal, not shared)
.claude/settings.local.json
